<div class="card shadow-sm border-0 mb-4">
  <div class="card-body bg-gradient bg-success bg-opacity-10 rounded-3 d-flex justify-content-between align-items-center p-4">
    <div>
      <h2 class="fw-bold text-success mb-1">
        <i class="bi bi-calendar2-week me-2"></i><%= @project.name %> シフト管理
      </h2>
      <p class="text-muted mb-0">日勤・夜勤のスケジュールを確認・作成できます</p>
    </div>
    <div>
      <%= link_to projects_path, class: "btn btn-outline-success d-flex align-items-center gap-2 fw-semibold" do %>
        <i class="bi bi-arrow-left-circle"></i> TOPへ戻る
      <% end %>
    </div>
  </div>
</div>
<div id="calendar"
     data-project-id="<%= @project.id %>"
     data-shifts="<%= @shifts.to_json %>"></div>