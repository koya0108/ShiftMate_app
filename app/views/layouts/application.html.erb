<!DOCTYPE html>
<html>
  <head>
    <title>ShiftMate</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <%= csrf_meta_tags %>
    <link rel="icon" href="/favicon.png" type="image/png">
    <%= csp_meta_tag %>

    <!-- 静的OGP設定 -->
    <%= display_meta_tags(
      site: 'ShiftMate',
      reverse: true,
      description: '1日の日勤・夜勤シフトを自動で作成するアプリ ShiftMate',
      keywords: 'シフト, 夜勤, 自動作成, 看護師, コールセンター, ShiftMate',
      og: {
        title: 'ShiftMate',
        description: '1日の日勤・夜勤シフトを自動作成。',
        type: 'website',
        url: 'https://shiftmate-app-f4dad2196d7c.herokuapp.com/',
        image: 'https://shiftmate-app-f4dad2196d7c.herokuapp.com/ogp-image.png',
        site_name: 'ShiftMate',
        locale: 'ja_JP',
      }) %>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <!-- bodyにアクション名クラスを付与 -->
  <body class="d-flex flex-column vh-100 <%= "#{controller.action_name}-page" %>">
    <%= render 'shared/header' %>

    <div class="d-flex flex-grow-1">
      <!-- ログイン画面は除外 -->
      <% unless controller_name == "sessions" && action_name == "new" %>
        <!-- サイドバー -->
        <%= render 'shared/sidebar' %>
      <% end %>

      <!-- メインコンテンツ -->
      <main class="flex-grow-1 p-3">
        <!-- トースト通知（右上表示） -->
        <div class="toast-container position-fixed top-0 end-0 mt-5 me-3" style="z-index: 2000;">
          <% flash.each do |key, message| %>
            <div class="toast align-items-center text-white 
                        bg-<%= key == "notice" ? "success" : "danger" %> border-0 show flash-toast"
                role="alert" aria-live="assertive" aria-atomic="true">
              <div class="d-flex">
                <div class="toast-body fw-semibold">
                  <%= message %>
                </div>
                <button type="button" class="btn-close btn-close-white me-2 m-auto"
                        data-bs-dismiss="toast" aria-label="閉じる"></button>
              </div>
            </div>
          <% end %>
        </div>

        <script>
          document.addEventListener("turbo:load", () => {
            const toastElList = document.querySelectorAll('.flash-toast');
            const toastList = [...toastElList].map(toastEl => new bootstrap.Toast(toastEl));
            toastList.forEach(toast => toast.show());
          });
        </script>

        <!-- show / confirm ページのステータスバナー -->
        <% if controller_name == "shifts" %>
          <% if action_name == "confirm" %>
            <div class="alert alert-success text-center fw-bold w-75 mx-auto" role="alert">
               このシフトは <strong>確定済み</strong> です
            </div>
          <% elsif action_name == "show" && @shift&.draft? %>
            <div class="alert alert-warning text-center fw-bold w-75 mx-auto" role="alert">
               このシフトは <strong>下書き中</strong> です
            </div>
          <% end %>
        <% end %>

        <%= yield %>
      </main>
    </div>
  </body>
</html>